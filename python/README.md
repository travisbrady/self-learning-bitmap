# Python Self-Learning Bitmap

The Python version requires cityhash and bitarray which can be installed like so:
```bash
$ pip install bitarray
$ git clone git@github.com:Amper/cityhash.git
$ cd cityhash
$ python setup.py install
```
